<item-detail>

  <div if={ props.item }>
    <h3>Item Details</h3>
    <div><b>ID:</b> { props.item.id }</div>
    <div><b>Name:</b> { props.item.title }</div>
  </div>
  <div else>
    <p>No Selected Items</p>
  </div>

  <script>
    export default {
      onBeforeMount(props) {
        console.log(props)
      }
    }
  </script>

</item-detail>
